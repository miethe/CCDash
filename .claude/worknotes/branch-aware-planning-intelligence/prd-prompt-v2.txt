Task: Author a Tier 2 PRD for **Phase 2** of Branch-Aware Planning Intelligence (multi-branch/worktree filesystem watching). This is the v2 follow-on to the already-shipped Phase 1 (display-from-existing-data). You are running in a clean context; read the files named below to get all design detail.

OUTPUT FILE (write it yourself, incrementally): docs/project_plans/PRDs/enhancements/branch-aware-planning-intelligence-v2.md

READ THESE FIRST (they hold all design detail):
- Design spec (PRIMARY — has the full architecture, OQs, migration v34, S2 correlation, risk table): docs/project_plans/design-specs/branch-aware-phase2-multi-branch-watcher.md
- R-01 feasibility brief: docs/project_plans/exploration/branch-aware-planning-intelligence/spikes/r01-branch-watcher/r01-branch-watcher-brief.md
- Spike leg findings: docs/project_plans/exploration/branch-aware-planning-intelligence/spikes/r01-branch-watcher/watcher-arch-findings.md and .../data-model-findings.md
- ux/risk/tech leg findings: docs/project_plans/exploration/branch-aware-planning-intelligence/spikes/ux-value-findings.md, .../risk-findings.md, .../tech-findings.md
- Exploration charter (disclosure constraints): docs/project_plans/exploration/branch-aware-planning-intelligence/branch-aware-planning-intelligence-charter.md
- Phase 1 PRD (anchor for style + what Phase 1 already delivered — DO NOT re-scope Phase 1 UX): docs/project_plans/PRDs/enhancements/branch-aware-planning-intelligence-v1.md
- ADRs: docs/project_plans/adrs/adr-006-db-authoritative-project-registry.md and docs/project_plans/adrs/adr-007-db-write-failure-surfacing-standard.md
- PRD TEMPLATE (follow its structure): .claude/skills/planning/templates/prd-template.md
- Frontmatter field reference: .claude/skills/artifact-tracking/schemas/field-reference.md

SCOPE — Phase 2 is INFRASTRUCTURE + CORRELATION first (per design spec §1, the chip/dialog UX stories were pulled into Phase 1 and are NOT re-scoped here). Three workstreams:
(1) BranchWatcherRegistry infrastructure — new class mirroring FileWatcherRegistry, keyed by (project_id, worktree_path), Option A from the design spec, scoped to docs/progress dirs only, sourced exclusively from planning_worktree_contexts rows (ADR-006 compliant; NO auto-discovery as a runtime binding path, NO per-branch git checkout scanning). Container registration, snapshot extension via a parallel branch_watchers key, register/unregister lifecycle, startup load of active rows, stop_all on shutdown.
(2) Data layer — DB migration v34: documents.branch TEXT DEFAULT '' column + idx_docs_project_branch + idx_sessions_git_branch_project; branch_filter param_extractor cache isolation on the four @memoized_query planning endpoints; ADR-007 retrofit of SqliteDocumentRepository.upsert (Phase 0); Postgres parity.
(3) S2 branch-signal correlation — _correlate_branch step in session_correlation.py with exclusion set, prefix normalization, min-length guard, medium confidence, Codex null-branch early-exit.
Frontend in v2 is LIMITED to: DEF-003 PlanningTopBar branch chip (reads existing worktree.branch, ~0.5-1pt, no backend change) plus any UX gap the design spec/brief verify did NOT ship in Phase 1. Do not re-author the Phase 1 chip/dialog stories.

MANDATORY — bake these FOUR R-01 preconditions in as explicit requirements/ACs (cite source = R-01 brief recommended_next_action):
1. ADR-007 retrofit for SqliteDocumentRepository.upsert scoped as a mandatory Phase-0 task (direct-count assertion test + lock-injection test + Postgres parity).
2. BranchWatcherRegistry lifecycle notification mechanism (planning service write path -> register/unregister) explicitly designed in a Data Contracts section, citing PROPOSED adr-008-branch-watcher-registry-planning-service-seam.md. This ADR does NOT exist yet — cite it as proposed; drafting+accepting it is a Phase-0 plan task. Surface OQ-1 (event mechanism: direct-call vs event-bus) as the key decision ADR-008 must record.
3. Startup sync serialization path specified (worktree watcher registration must serialize against startup sync to avoid races; see OQ-3).
4. AC for Codex null-branch disclosure (Codex sessions hardcode NULL git_branch; UI/correlation must disclose/handle this honestly, not hide it).

MANDATORY — carry the FOUR charter disclosure constraints as non-functional ACs: (a) cwd lives in a JSON blob (parse defensively); (b) worktree contexts are operator-gated (no silent auto-registration); (c) SSE/live-update is in-process-only under SQLite (no cross-process fanout); (d) Codex structural null-branch.

RESILIENCE (project rule): every new optional backend field needs an explicit "FE handles missing X" AC. The documents.branch column defaults to '' — specify query/UI behavior when branch is empty/null.

PERFORMANCE: document the N<=5 supported operational range; N=10+ requires write-amplification profiling (OQ-5) before any scale-out. Note this profiling as a verification task, not a blocker.

DEFERRED ITEMS: note command-center-detail-panel-consolidation (docs/project_plans/design-specs/command-center-detail-panel-consolidation.md) as deferred/related — full side-pane vs board-modal consolidation is OUT of scope for v2. Also note OQ-6 (exact-match high-confidence promotion) as deferred tuning.

FRONTMATTER (schema v2): doc_type prd, title "Branch-Aware Planning Intelligence v2 — PRD", feature_slug branch-aware-planning-intelligence, feature_version v2, status draft, priority high, risk_level medium, changelog_required true, category enhancements, created '2026-06-11', updated '2026-06-11'. related_documents must include the R-01 brief, charter, design spec, and Phase 1 PRD. prd_ref null; plan_ref null. spike_ref the R-01 brief. adr_refs include 006, 007, and proposed 008.

Keep the PRD <= 800 lines. Phased breakdown should reflect: Phase 0 (ADR-007 retrofit + draft/accept proposed ADR-008), data layer (migration v34 + cache), BranchWatcherRegistry infra, S2 correlation, frontend (DEF-003 chip), verification (+ N<=5 profiling), docs (CHANGELOG + operator guidance).

Deliverable: the written PRD file. End your run by printing the file path and a 6-line summary (phases + total point estimate). Complete in a focused pass; do not over-iterate.