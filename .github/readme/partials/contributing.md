---

## Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/my-feature`)
3. Make your changes
4. Run tests: `npm run test` (frontend) and `backend/.venv/bin/python -m pytest backend/tests/ -v` (backend)
5. Open a pull request
