# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
.pytest_cache/
.venv*/**

examples/*
# Database
*.db
*.db-shm
*.db-wal
data/
build/

# Load-test / profiling artifacts
artifacts/

.env

# Local machine state (untracked; see .env.example for template)
projects.json

# Local streaming worker: real env lives at ~/.ccdash/stream.env (outside repo).
# Guard against an accidental real secret file inside the repo; the *.example is tracked.
deploy/local-streaming/stream.env

# Claude Code worktrees (live git worktrees, never commit as files)
.claude/worktrees/
.claude/scheduled_tasks.lock

# execute-plan wave rollback checkpoints (local orchestration state)
.claude/progress/*/.wave-*-checkpoint

# Worktree-placement probe verdict cache. PER-MACHINE measurement of one Claude
# Code version by one probe shape -- must never be committed: a stale or wrong
# `inherits` propagated to another machine suppresses the very probe that would
# catch a harness regression, and the failure mode is silent (a run reports
# `isolation: worktree` while its agents commit in the main checkout).
.claude/.autopilot-worktree-capability.json
